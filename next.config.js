/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  // Add this to ensure proper static export
  distDir: 'out'
}

module.exports = nextConfig